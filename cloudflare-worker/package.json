{
  "name": "clockin-sync-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for local + cloud sync",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.12.0"
  }
}
